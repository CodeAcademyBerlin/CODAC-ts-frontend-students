mutation createLmsFeedback($rating: Int!, $slug: String!) {
  createLmsFeedback(data: { rating: $rating, slug: $slug }) {
    data {
      attributes {
        rating
        slug
      }
    }
  }
}
